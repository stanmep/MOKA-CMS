@{
	ViewBag.Title = "Login";
}

<div class="container-fluid">
	<div class="row-fluid">
		<div class="offset4 span4">
			<div class="row-fluid">
				<div class="span12">
					<h1>@Html.Raw(ViewBag.Title)</h1>
				</div>
			</div>

			<div class="row-fluid">
				<div class="span12">
					<form id="login-form" class="form-horizontal" method="post" action="/account/login">
						<div class="row-fluid">
							<div class="controls span12">
								<input type="text" id="username" class="span12" name="username" placeholder="Username" />
							</div>
						</div>
						<div class="row-fluid margin-top-10">
							<div class="controls span12">
								<input type="password" id="password" class="span12" name="password" placeholder="Password" />
							</div>
						</div>
						<div class="row-fluid margin-top-10">
							<div class="controls span12">
								<label class="checkbox">
									<input type="checkbox" id="remember-me" name="remember-me" /> Remember me
								</label>
							</div>
						</div>
						<div class="row-fluid margin-top-10">
							<div class="controls span12">
								<input type="submit" id="signin" class="btn btn-primary span12" name="signin" value="Sign in" />
							</div>
						</div>
					</form>

				</div>
			</div>

			<div class="row-fluid">
				<div class="span12 text-center">
					@if (IsPost)
					{
						if (ViewBag.Authenticated)
						{
							<div id="login-message" class="alert alert-success">
								Welcome, @Html.Raw(ViewBag.Username)!
							</div>
						}
						else
						{
							<div id="login-message" class="alert alert-error">
								Ooos! Try again.
							</div>
						}
					}
					else
					{
						<div id="login-message" class="alert alert-info">
							Not signed in
						</div>
					}
				</div>
			</div>
		</div>
	</div>
</div>

@section js
{
	<script type="text/javascript">
		var users = [{
				"username": "admin",
				"password": "admin",
				"email": "admin@mokacms.org"
			}
		];	// JSON array.
	</script>
}
